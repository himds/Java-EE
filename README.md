# Water Quality Project

Structure:
- `docker/docker-compose.yml` : MySQL + Tomcat
- `database/schema.sql` : database schema
- `backend/` : Jakarta EE REST API + ETL
- `frontend/` : Leaflet map UI
- `data/` : TXT OpenData files
